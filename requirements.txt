# Achilles - Calibre Quote Tracker Requirements
# Python version: 3.6+

# === Core Metadata Analyzer (no dependencies) ===
# - sqlite3 (included in Python)
# - argparse (included in Python)
# - pathlib (included in Python)
# - collections (included in Python)
# - json (included in Python)

# === Quote Tracker Extensions ===

# Text Extraction
PyMuPDF>=1.23.0      # PDF text extraction
ebooklib>=0.18       # EPUB reading
beautifulsoup4>=4.12.0  # HTML/XML parsing
lxml>=4.9.0          # XML handling

# Web UI
streamlit>=1.29.0    # Interactive web interface

# Natural Language Processing
nltk>=3.8.0          # Sentence tokenization for context extraction

# Semantic Search (RAG Integration)
chromadb>=0.4.22     # Vector database for semantic search
sentence-transformers>=2.3.0  # Text embeddings for semantic similarity
